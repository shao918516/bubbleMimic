# xGMR / dex_evt2 全身动作模仿项目 — 问题排查与优化总结

本文档梳理这几天围绕 `dex_evt2` 机器人、从 BVH 动作重定向（xGMR）到 IsaacLab + RSL-RL 强化学习训练全链路排查出的问题、根因分析、解决方案与实际效果。按"重定向阶段"和"训练阶段"两大部分组织，每个问题遵循"问题描述 → 原因分析 → 解决思路 → 最终效果"的结构。安装依赖、环境配置一类的琐碎问题不在此列。

---

## 第一部分：动作重定向阶段（xGMR / `bvh_to_robot.py`）

### 1.1 根部姿态重置逻辑：从"没有真正生效"到"用错了坐标系"

**问题描述**
`bvh_to_robot.py` 主循环里，为了避免前两帧 IK 收敛未稳定导致的抖动，设计了"重置根部位姿到参考数据"这一逻辑。但实际运行时，即使加了这段逻辑，前几帧的抖动问题依然存在。

**原因分析（分两层，逐步剥开）**
- **第一层**：原始脚本这段代码写的是 `retargeter.configuration.data.qpos[:3] == lafan1_data_frames[i]["Hips"][0].copy()`——用的是 `==`（比较运算符）而不是 `=`（赋值）。这是一处笔误，导致这行代码只是做了一次比较然后把结果丢弃，"重置根部位姿"这个意图从未真正执行过，是彻头彻尾的死代码。
- **第二层**：把 `==` 改成 `=` 之后，又暴露出更深的问题——赋值用的数据源 `lafan1_data_frames[i]["Hips"]` 是 BVH **原始、未缩放**的人体动作数据，而 retarget 内部的 IK 求解用的是**经过按人体/机器人身高比例缩放**之后的数据（存在 `retargeter.scaled_human_data` 里）。通过把两者的四元数换算成"绕哪个轴转了多少度"直接对比，发现 raw 和 scaled 两份数据的旋转转轴、转角完全不同（差了 70° 以上），确认这不是"差一点点"的误差，是两套完全不在同一坐标系下的数据。直接把原始数据硬塞进机器人的 `qpos`，等于把机器人内部状态瞬间"传送"到一个跟自身尺度完全不匹配的位置，而且这个错误状态还会作为下一帧 IK 求解的热启动起点，把误差传染下去。

**解决思路**
将赋值源从 `lafan1_data_frames[i]["Hips"]` 改为 `retargeter.scaled_human_data["Hips"]`，确保重置时使用的数据跟 IK 内部工作的坐标系是同一套。

**最终效果**
根部重置逻辑第一次真正按设计意图工作；后续所有排查均建立在这个修复之上。

---

### 1.2 主循环内三处数据对齐 / 计算逻辑 bug

**问题描述**
对比用户修改版 `bvh_to_robot.py` 与项目原始脚本（"复件"版）时，发现原始脚本本身也带着几处隐藏 bug。

**原因分析**
1. **一帧错位**：原脚本里 `qpos_list.append(qpos)` / `end_poses.append(end_pos...)` / `dof_vels.append(dof_vel...)` 这几行，执行顺序在 `end_pos`、`dof_vel` **被重新计算之前**，导致每次存入列表的 `end_pos`/`dof_vel` 实际上是"上一帧"的残留值，跟同一次 append 里的 `qpos`（当前帧）对不上，整条数据错位一帧。
2. **速度参考点从未更新**：`pre_dof_pos` 只在第一次（`i=0`）被赋值，之后的所有帧都没有再更新它。这导致后续每一帧算出来的"速度"，实际上是"当前帧 - 第 0 帧"再除以 dt，随着帧数增加数值会持续增大、彻底失去"帧间速度"的物理意义。
3. **对四元数直接做差分**：原脚本对包含根部四元数的完整 `qpos` 做差分来算速度，而四元数之间直接相减、除以 dt 并不是合法的角速度计算方式（正确做法需要用四元数的相对旋转再转换成角速度），且这样算出来的"速度"数组维度也跟真正的关节速度维度对不上。

**解决思路**
- 把 append 逻辑挪到 `end_pos`/`dof_vel` 重新计算完成之后，保证三者基于同一帧对齐。
- 让 `pre_dof_pos` 每一帧都更新为当前帧的值。
- 只对 `dof_pos`（关节角，不含根部位置/四元数）做差分来算速度。

**最终效果**
这几处虽然是真实 bug，但由于用户当次命令输出的是 `.pkl` 格式（不包含 `dof_vel`/`end_pos` 字段），当时没有直接影响输出结果，属于"提前修好、为后续保存 `.npz` 或做可视化调参打好基础"的修复。

---

### 1.3 核心问题：`dex_evt2` 被硬编码复用 `dex_evt`（v1）的 IK 配置

这是整条重定向排查链路里影响最大、耗时最长的问题，从"关节速度经常离谱超限"这个现象出发，经过多轮排查才最终定位。

**问题描述**
`dex_evt2`（37 自由度含浮动基座、31 个实际可控自由度）retarget 出来的动作，关节角速度经常远超合理范围（个别关节峰值达到限幅数值的十几到三十倍），且集中出现在手臂/手腕相关自由度上，某些关节的表现又诡异地被"锁死"在很小的范围内来回跳变。

**原因分析（多轮排查，逐步收窄范围）**
- 最初怀疑速度限幅（`VelocityLimit`）没有生效，用残差检查（`G@vel - h`）直接验证了这一点：求解出的速度确实违反了限幅约束本身生成的不等式条件，大量约束行被违反。
- 进一步用完全独立于项目代码的最小复现脚本（只用 mink + mujoco 原生接口）复现同样的现象，证实这不只是项目代码的问题。
- 但同时发现，`dex_evt2` 有一处"贴顶"现象特别规律：手腕两个自由度的速度值精确等于"关节 range 宽度 / dt"，说明这两个关节被 XML 里 `range="-0.01 0.01"` 这样窄到几乎锁死的物理限位卡住——顺着这条线追下去，才发现真正的核心问题：`motion_retarget.py` 里有一行硬编码分支——
  ```python
  if "dex_evt2" in tgt_robot:
      config_path = IK_CONFIG_DICT[src_human]["dex_evt_" + format]
  ```
  `dex_evt2` 的骨架模型（MJCF/URDF）用的是它自己独立的资产文件，但 IK 目标映射表（人体关键点 → 机器人 body 的对应关系、位置/姿态目标权重）却被写死复用了 `dex_evt`（v1，23 自由度、无可动头部、4 自由度手臂）的配置。v1 和 v2 的连杆长度、自由度数量、部分关节结构并不完全一致，用按 v1 骨架调好的目标映射去解 v2 骨架的 IK，必然会有部分关节"追不到目标、拼命超调"，另一部分关节因为映射错位、目标离得极近而几乎不动——这跟观察到的"有的关节狂飙、有的关节死死卡住"两极分化现象完全吻合。

**解决思路**
- 去掉这处硬编码分支，让 `dex_evt2` 走通用查表逻辑，使用自己专属的配置文件（新建 `bvh_lafan_to_dex_evt2.json`、`bvh_noitom_to_dex_evt2.json`、`bvh_xsens_to_dex_evt2.json`，并在 `params.py` 里正确注册）。
- 新配置基于原 v1 配置的结构改造，人体侧（关键点名字、缩放表）保持不变，机器人侧的目标映射经过核对确认所有引用的 body 名称在 v2 骨架上都存在。

**最终效果**
从"骨架用 v2、目标映射用 v1"这种根本性的错配中解脱出来，是后续一切速度/精度相关优化能够继续推进的前提。

---

### 1.4 速度限幅数值：录入错误与缺失

**问题描述**
`robot_velocity_limits` 里部分数值明显有问题：左右对称的关节，右侧数值离谱地比左侧大出六七倍（如 `shoulder_pitch_r_joint: 35.184` vs 左侧 `5.184`）；同时 `dex_evt2` 独有的几个关节（`elbow_yaw`、`wrist_pitch`、`wrist_roll`）在原配置里完全没有登记限幅数值。

**原因分析**
- 左右不对称的数值，结合整张表其余所有左右关节对的数值都严格一致这一点判断，是数据录入时的笔误（很可能是多打了一个开头的"3"）。
- 缺失的几个关节，是因为它们是 v2 相对 v1 新增的自由度，原配置从未涵盖过。
- 后续找到了 `dex_evt.py`（真实的 IsaacLab 机器人仿真配置文件）里，`DelayedImplicitActuatorCfg` 中登记的 `velocity_limit_sim` 数值精确到十几位小数（如 `13.82300767579509`），一望而知是从电机原始参数换算而来，可信度远高于此前的估算值或录入有误的旧数据，遂用这份数据统一替换了所有 `dex_evt2` 配置里的 `robot_velocity_limits`。

**解决思路**
用 `dex_evt.py` 里的权威数值统一更新三份 `dex_evt2` 配置文件的速度限幅，左右完全对称（因为原始数据本身就是用同一条正则同时匹配左右两侧），不再存在人工录入不对称的风险。头部两个关节因为 `dex_evt.py` 完全没有相关配置（v1 头部是固定关节，不可动），也没有任何 IK 目标驱动头部，予以保留为空、不编造数值。

**最终效果**
限幅数值来源可信度大幅提升，且不再有对称性错误。

---

### 1.5 mink 库 `VelocityLimit` 本身在浮动基座模型上失效（已确认的库级问题）

**问题描述**
即使 IK 配置和限幅数值都修正无误，`retarget()` 内部 `solve_ik()` 解算出的关节速度依然大幅超出配置的限幅（残差检查显示大量约束行被违反，违反幅度可达数十到接近三百）。

**原因分析**
- 用直接读取 `VelocityLimit.compute_qp_inequalities()` 生成的不等式约束、与实际解出的速度做残差比对（`residual = G @ vel - h`），证实约束确实被违反，不是数值噪声级别的偏差。
- 为排除"是这个项目自己代码在某处干扰了限幅"这一可能，编写了完全独立、不依赖项目任何自定义代码（不涉及 `GeomDistanceLimit`、`IK_CONFIG_DICT`、多任务表等）的最小复现脚本，只用 `mujoco` + `mink` 原生接口加载 `dex_evt2` 的模型文件，构造一个简单的远距离目标任务，配一个统一的速度限幅，跑几步观察结果——同样的违反现象立刻复现（第一步就冲到限幅的 27 倍）。
- 进一步把求解器从 `daqp` 换成 `quadprog`（两种原理完全不同的 QP 求解算法），结果精确到小数点后四位完全一致——这说明问题不在求解器这一层，两个求解器只是"忠实地"解出了同一个已经有问题的 QP 问题，嫌疑集中到 `VelocityLimit` 构造约束矩阵这一步本身，很可能与浮动基座模型下 `nq`（位置维度）与 `nv`（速度/切空间维度）不一致（7 vs 6）的索引处理有关。

**解决思路**
- 短期：在 `bvh_to_robot.py` 里对 `retarget()` 的输出做"事后硬裁剪"——按配置的限幅，把当前帧相对上一帧的关节角变化量强制裁剪到限幅允许的范围内，并把裁剪后的结果通过 `configuration.update()` 写回内部状态，保证下一帧的热启动也是基于裁剪后的、物理上合理的状态，不让超速状态持续累积。
- 长期：整理成一份可复现、可直接提交的问题报告（含最小复现脚本、两种求解器结果对比、残差检查结果），建议向 mink 上游反馈。

**最终效果**
兜底裁剪生效后，最终产出数据里每个关节的最大速度都精确贴合配置限幅（不多不少，验证过是"精确卡住"而非巧合），彻底解决了之前"速度爆表数十倍"的问题，实现了数值上的绝对安全。库本身的问题目前仍未从根本解决，但已被绕开，不影响下游使用。

---

### 1.6 直接修改 `qpos` 未触发正向运动学缓存刷新

**问题描述**
即使有了兜底裁剪，个别情况下相邻帧之间的行为依然不如预期稳定。

**原因分析**
无论是根部重置还是兜底裁剪，代码里都是直接对 `retargeter.configuration.data.qpos[...]` 赋值。但 mink 的 `Configuration` 对象内部缓存的正向运动学结果（各 body 的世界坐标位置/姿态，IK 任务计算 Jacobian 和误差时依赖这些缓存值）并不会随着对 `qpos` 的直接赋值自动刷新——必须显式调用 `configuration.update(q)` 才会重新执行一遍正向运动学。直接赋值会导致 `qpos` 本身是对的，但下一次 `retarget()` 调用时，IK 内部依赖的缓存数据仍然是修改前的旧值，出现内部状态不一致。

**解决思路**
把所有直接赋值 `data.qpos` 的地方，统一改成先构造完整的目标 qpos，再调用 `configuration.update(qpos)`。

**最终效果**
消除了这一处内部状态不一致的隐患，是兜底裁剪机制能够真正按预期稳定工作的必要前提。

---

### 1.7 训练数据格式转换（`gmr_to_npz_inter.py`）与骨架自由度数量确认

**问题描述**
`gmr_to_npz_inter.py`（把 retarget 出的 `.pkl` 转换成 IsaacLab 训练用 `.npz` 的脚本）的 `--robot` 参数只接受 `dex_evt`，不接受 `dex_evt2`，一开始怀疑这是又一处"漏掉了 dex_evt2 支持"的遗留问题。

**原因分析**
仔细阅读脚本源码后发现，这不是配置遗漏，而是真实的机器人物理结构差异：`dex_evt`（v1）在这个训练框架里对应的 IsaacLab 机器人定义（`ROBOT_CONFIG_DEX_EVT`）确实只有 23 个自由度——没有可动头部，手臂只到 `elbow_pitch` 为止，没有 `elbow_yaw`/`wrist_pitch`/`wrist_roll`。但脚本内部已经有一套按输入维度自动匹配关节顺序的机制（`input_joint_orders: {29: ..., 31: DEX_EVT_INPUT_JOINT_ORDER_WITH_HEAD}`），会按**关节名字**（不是位置索引）把 `dex_evt2` 的 31 维数据里，与 v1 相同的 23 个关节安全提取出来，多出的 8 个（头部 2 个 + 每条手臂 3 个）会被显式丢弃并打印提示，不会导致数据错位。

**解决思路**
确认无需修改代码，`--robot dex_evt` 对这类转换任务本身是正确用法，只是意味着头部和手腕后三个自由度的动作数据不会进入最终训练数据。是否需要真正支持训练全部 31 个自由度，取决于 IsaacLab 那边有没有对应的、启用了全部自由度的机器人仿真配置（目前 `dex_evt.py` 里这几个关节的执行器配置是注释掉的，需要额外补全 PD 增益等参数才能真正启用）。

**最终效果**
避免了一次不必要的"改代码"操作，同时明确了当前训练数据的实际自由度覆盖范围。

---

## 第二部分：强化学习训练阶段（IsaacLab + RSL-RL）

### 2.1 `_last_torque` 缓存未按 episode 边界重置

**问题描述**
`joint_torque_smooth_reward` 长期精确等于 `0.0000`，怀疑没有真正提供学习信号。

**原因分析**
该 reward 用 `asset._last_torque` 缓存上一步的力矩来计算帧间变化量，但这个缓存是所有并行环境共用的同一个属性，且从未在任何 `reset` 阶段被清零。由于各并行环境是异步 reset 的，某个环境 episode 刚开始时，读到的"上一步力矩"其实是它自己上一段 episode 结束时（可能是完全不同的姿态）的力矩，产生的是两个不相关状态之间的虚假跳变，长期把 `exp(-smooth_error/std²)` 压到数值意义上的 0。

**解决思路**
新增 `reset_last_torque` 函数，按 `env_ids` 对相应环境的力矩缓存清零，注册为 `mode="reset"` 的 `EventTerm`。为验证机制是否真正生效，额外加了调试打印，确认这个重置事件确实按预期的频率（约等于"总环境数 / 平均 episode 长度"）在触发。

**最终效果**
确认重置逻辑本身工作正常（这一点后来在排查 `torque_smooth` 长期为 0 的另一个原因——`smooth_std` 数量级问题时，起到了排除干扰项的作用）。

---

### 2.2 六个 `exp()` 类追踪 reward 长期饱和在 0

这是训练侧影响最大的问题：**从训练一开始到问题被发现为止，用于衡量"动作追踪得准不准"的核心奖励信号，实际上从未向策略提供过任何有效梯度。**

**问题描述**
`Episode_Reward/motion_global_anchor_pos` 等四项，日志里长期显示 `0.0002` 左右，几乎不随训练变化。

**原因分析**
这几项 reward 的计算方式都是 `exp(-error² / std²)`（部分还乘了 0.6/0.7 的系数进一步收紧）。把训练日志里实际达到过的误差数值代入公式验算：即使是全部训练日志里记录过的最好一次结果（`error_anchor_pos = 0.0815`），算出来的 reward 也只有 `1.2×10⁻⁹`，在 float32 精度下与 0 无异；训练早期误差更大时，指数部分甚至达到 `-560` 这个量级，`torch.exp()` 在这种输入下会直接下溢成精确的 `0.0`。也就是说，`std` 被设定得比实际能达到的误差范围小了一到两个数量级，导致这几项 reward 无论策略表现好坏，输出值都趋近于 0，训练全程处于"没有真正的追踪信号"的状态。

**解决思路**
把四项主要位置/姿态类 reward 的 `std` 按"新的有效 std 大致贴近实测误差量级"这一原则重新估算，普遍放宽约 8~10 倍（如 `anchor_pos`: 0.03 → 0.25）。这只是一个"让梯度先跑起来"的合理起点，明确告知需要后续根据训练进展继续微调，理想情况下配合课程学习让 `std` 随训练进度逐步收紧。

**最终效果**
四项 reward 数值从 `0.0002` 级别提升到 `0.001~0.006` 级别，不再是精确的 0。但也发现速度类的另外两项 reward（`motion_body_lin_vel`/`motion_body_ang_vel`）存在完全相同的问题（当时被遗漏，后续单独补上，详见 2.4）。

---

### 2.3 `torque_over_limit` 惩罚量级失衡，架空追踪类 reward

**问题描述**
即使前一项修复后，追踪类 reward 的绝对数值依然很小（0.001~0.02 量级），而 `torque_over_limit` 单项经常达到 `-1~-4.5`。

**原因分析**
`torque_over_limit` 用的是 `torque_sum_excess` 函数——没有上限的线性惩罚（超出阈值多少就扣多少），跟其余六个基于 `exp()`、天然被压缩在 `[0,1]` 区间内的追踪类 reward，根本不是同一种数量级。实测这一项的原始超限量常年在 15~75 这个区间，配上原本 0.06 的权重，单这一项对总 reward 的贡献就能达到 -1 到 -4.5，比全部六个追踪 reward 加起来还大出百倍以上——原本"50% 权重给追踪保真度"的设计意图被这一项完全架空，策略几乎只能靠"少扣分"（降低力矩、减少动作幅度）而不是"贴着参考动作走"来提升 reward。

**解决思路**
把 `torque_over_limit` 的权重从 0.06 降到 0.002，把量级重新拉回跟追踪类 reward 可比的范围（约 -0.03~-0.15），不是取消这一项的作用，只是纠正它跟其他项之间的相对比重。

**最终效果**
惩罚项和追踪信号的量级关系回到合理区间，是后续能观察到 episode 里追踪误差真正开始下降的重要前提之一。

---

### 2.4 完全没有奖励覆盖的关节空间速度追踪

**问题描述**
排查发现，`error_joint_vel`（关节空间角速度误差）持续恶化，同时 `error_body_ang_vel`（笛卡尔空间角速度误差）也在变差，但同期位置类误差在稳步改善。

**原因分析**
逐一核对 `RewardsCfg` 里全部 reward 项后发现，`motion_global_body_linear_velocity_error_exp`/`motion_global_body_angular_velocity_error_exp` 追踪的是各身体部件在**世界坐标系下的笛卡尔速度**，跟 `error_joint_vel`（**关节空间**的角速度）是两个不同的物理量，后者从训练一开始就没有任何 reward 项直接提供优化压力，只是作为 `Metrics` 被记录下来，纯粹处于"自然漂移、没人管"的状态。此外，前述"六项饱和"里被漏掉的 `motion_body_lin_vel`/`motion_body_ang_vel` 两项，同样经过实测误差量级代入验算，确认存在同样的 `std` 过紧问题（`exp()` 指数达到 `-759` 甚至 `-5170`，同样直接下溢为 0）。

**解决思路**
- 新增 `motion_joint_velocity_error_exp` 函数，直接对比参考动作与机器人的关节角速度，补上这条此前完全空白的信号，`std` 同样按实测误差量级估算。
- 把此前漏掉的 `motion_body_lin_vel`/`motion_body_ang_vel` 两项 `std` 一并放宽。

**最终效果**
`error_joint_vel`、`error_body_ang_vel`、`error_anchor_ang_vel` 等此前持续恶化或停滞的速度类指标，在后续训练中转为持续改善。

---

### 2.5 `joint_torque_smooth_reward` 的 `smooth_std` 同样过紧

**问题描述**
即使确认 `_last_torque` 重置机制工作正常（2.1），`torque_smooth` 这一项依然长期精确等于 `0.0000`。

**原因分析**
在函数内部加调试打印，直接测量 `smooth_error`（31 个关节力矩帧间变化量的平方和）的真实数值，发现常年稳定在 8.5 万~12.4 万这个量级（个别峰值到 30~70 万），而原来配置的 `smooth_std=0.2` 对应的分母只有 `0.04`——两者相差接近六个数量级，`exp(-124000/0.04)` 无论如何都会下溢为精确的 0，跟"没有被重置"这个此前排除掉的假设无关，是纯粹的数量级设定错误。

**解决思路**
按实测量级把 `smooth_std` 从 0.2 调整到 300。

**最终效果**
`torque_smooth` 终于脱离精确的 0，开始随训练变化。这次排查过程中顺带发现，力矩帧间变化量之所以数量级如此之大（意味着策略输出的力矩本身波动剧烈），可能与 `rsl_rl_ppo_cfg.py` 里 `clip_action=100` 这个几乎不起裁剪作用的宽松设定有关，作为后续可能需要关注的方向记录下来，暂未处理。

---

### 2.6 `entropy_coef` 过小导致训练早期探索能力"崩塌"

**问题描述**
`Mean action std` 从初始值 0.98 持续、稳定地下滑，几百轮后卡死在 0.12~0.15 附近不再变化，`Mean entropy loss` 变为很大的负数（-12~-17），且这个阶段对应的追踪误差、`episode length` 也长期停滞不再改善。

**原因分析**
最初怀疑是 PPO 的自适应学习率（`schedule="adaptive"`，根据新旧策略 KL 散度动态调整学习率）在训练早期把步子迈得过大、加速了这个过程。从 TensorBoard 导出学习率随迭代次数变化的完整数据后逐段核实：学习率确实经历过短暂的初期波动，但早在 `std` 真正开始大幅崩塌之前（约第 450 轮左右）就已经稳定在一个较小、基本不再变化的区间，后续三千多轮里始终维持这个稳定水平——排除了"学习率持续失控"这个假设。据此判断，真正原因是当前 `entropy_coef=0.005` 这个系数，不足以抵消策略/价值网络损失本身持续存在的"倾向于降低动作方差、变得更确定"的梯度方向，哪怕学习率保持稳定、每步移动幅度不大，几千次迭代累积下来也足以把探索噪声磨到很小。

**解决思路（一次实验-验证循环）**
- 先尝试把 `entropy_coef` 直接提高 4 倍到 0.02（跳跃式调整而非小步试探，目的是尽快拿到方向明确的信号）——结果矫枉过正，`std` 冲过初始值 1.0，涨到 1.27 仍在继续上升，同期速度类误差全线创下训练以来的新高，位置类误差反而在变好——判断为"噪声过大、动作变得毛躁，均值仍能对上目标但瞬时速度失真"。
- 取两次结果的中间值，改为 `entropy_coef=0.01`，`std` 的下降速率明显放缓（呈现逐渐减速、趋于平稳的正常收敛曲线，不是持续加速下滑）。
- 后续用户根据某次单独观察到的效果一度改回 0.005，之后在切换训练动作、叠加终止阈值课程学习的过程中，同样的"`std` 早早卡死、误差停滞不前"的现象再次出现，判断很可能还是同一个问题——最终决定采用 `entropy_coef=0.01` 与终止阈值课程学习（见 2.8）组合，作为目前证据支持最充分、但尚未完整验证到收敛的方案。

**最终效果**
`entropy_coef=0.01` 单独使用时展现出健康的、逐渐放缓的探索衰减曲线；与课程学习组合后的完整效果，在本文档撰写时仍在验证中。

---

### 2.7 `episode length` 持续下降的规律：`anchor_pos` 与 `ee_body_pos` 的此消彼长

**问题描述**
无论训练哪个动作，反复观察到同一种模式：训练早期主要因为 `anchor_pos`（根部/整体姿态失衡）而提前终止；一旦这项能力学会，压力几乎立刻转移到 `ee_body_pos`（脚踝高度偏差），后者迅速占到 90%~98% 的终止原因，`episode length` 也随之大幅缩短。

**原因分析与诊断手段**
为了区分"是物理限位卡住了脚踝、调什么参数都没用"还是"策略本身还没学会"，设计了两个低成本诊断：
1. 新增 `ankle_torque_saturation_diag`/`ankle_velocity_saturation_diag` 两个仅用于记录（权重设为接近 0，不影响训练）的诊断项，实时监控踝关节实际力矩/速度相对各自物理限位的比值，长期出现在 TensorBoard 里供观察。
2. 直接从参考动作数据本身，一次性打印脚踝在整段动作里的真实高度变化范围和相邻帧最大高度跳变量，与终止条件用的 0.4m 阈值做对比，判断这个阈值本身对当前动作是否合理。

**解决思路**
这两项诊断为下一步（2.8 的课程学习）提供了判断依据：如果长期贴近物理限位，说明是硬件能力瓶颈；如果参考动作本身高度变化范围远小于 0.4m 阈值，说明瓶颈不在阈值宽松度而在策略本身的精度。

**最终效果**
诊断机制已部署，具体数据尚待用户实际跑一段时间后回收分析；这一环节主要价值在于把"脚踝问题到底出在硬件限位、动作数据本身还是策略训练"这三种可能性用可验证的数据分开，避免继续凭经验猜测。

---

### 2.8 终止阈值课程学习（`CurriculumCfg` 从空白到实装）

**问题描述**
固定的终止阈值（无论 0.25 还是后来放宽到的 0.4）在训练早期都显得过于严格：策略还很粗糙时，稍有偏差就会被判定失败、episode 提前结束，PPO 能拿到的有效学习信号（尤其是需要走到动作后段才会遇到的片段）也随之变少，形成"越训越短"的负反馈。

**原因分析**
`CurriculumCfg` 这个 IsaacLab 内置的课程学习机制此前一直是空的，从未被使用过。这类"训练早期放宽约束、随进度逐步收紧"的做法是运动模仿类强化学习里的标准技巧，此前一直缺失。

**解决思路**
参照 IsaacLab 官方文档里 `modify_reward_weight` 这一内置课程学习工具的实现模式（确认可用的官方接口，只是用于调整 reward 权重），实现了一个结构相同、但作用于终止阈值并支持线性插值（而非官方工具默认的"到点即跳变"）的新课程项 `anneal_termination_threshold`：
- `ee_body_pos` 阈值从 0.8 线性收紧到 0.4，用时约 8000 个仿真步（约合 333 轮训练）。
- `anchor_pos` 阈值最初同样设置为从 0.6 收紧到 0.4（8000 步），但后续发现这项设置有反作用（见下）。

**中途发现的意外副作用与调整**
上线后 `ee_body_pos` 主导终止的问题被显著解决（终止占比从 95% 降到 1.4% 左右，`episode length` 从十几步回升到 70~90 步）。但同时发现，位置/姿态类追踪误差反而比不用课程学习时更差——回溯数据发现，`anchor_pos` 这项能力在完全不做任何阈值放宽的情况下，本来就能在 100~200 轮左右自然收敛得很好；这次连它也一起放宽，训练早期该项的学习压力被削弱，等阈值收紧回 0.4 后，策略还没有真正追上，导致几乎每一局都卡在这道坎上，比完全不加这部分课程学习时的表现还差。据此把 `anchor_pos` 的课程收窄（起始阈值 0.6→0.5，收紧时长 8000→3000 步），让这项本来学得快的能力尽早恢复到接近满强度的训练压力。

**最终效果**
`ee_body_pos` 部分的课程学习效果明确、显著；`anchor_pos` 部分从"帮倒忙"调整为"收窄后的版本"，实际效果和是否需要进一步调整，仍在等待新一轮训练数据验证。

---

### 2.9 训练动作的筛选与替换

**问题描述**
最初用于全流程排查的 `dance1_subject2`（LAFAN1 数据集"舞蹈"类别）本身是一段高动态动作，即使把前述所有已知问题全部修复，训练仍然持续表现出"根部学得不错、脚踝时机严重跟不上"的模式，短时间内难以收敛到能够稳定完成整段动作的程度。

**原因分析**
用已经验证过可靠的 `check_raw_retarget_pkl.py` 诊断脚本，对用户手上全部候选动作数据（覆盖 walk / run / sprint / aiming 等类别、共二十余个片段）做批量筛查，重点比较"根部线速度峰值/均值"和"相邻帧关节角跳变超过 5° 的帧数占比"两项指标，量化评估每段动作本身对当前训练成熟度而言的"好训程度"。同时结合评审规则（动作保真度、物理合理性、稳定性、专家评审四项权重）排除了舞蹈、格斗、跳跃、摔倒类、障碍类等已知高风险类别。

**解决思路**
筛选出 `walk3_subject3`、`walk4_subject1`（数据上最平稳的两个候选）与 `aiming1_subject1`（单帧最大跳变全场最低、同时上肢有一定动作幅度、可能对专家评审的表现力有帮助）三个候选，优先在 `aiming1_subject1` 上验证前述全部训练侧修复的实际效果。

**最终效果**
切换动作后，`episode length`、追踪误差等指标的整体表现明显优于同等训练轮数下的 `dance1_subject2`，验证了"先用可靠的动作把整套训练流程走通、再视时间是否充裕挑战更难的动作"这一策略的合理性。

---

## 第三部分：过程中的经验教训——文件版本不同步

这几天的排查过程中，反复出现同一类问题：本地实际运行的代码版本与讨论中确认过的最新版本不一致，包括但不限于——
- 已经确认改成 `retargeter.scaled_human_data` 的根部重置逻辑，在后续某次"整个项目重新梳理"时又被回退成了旧的 `lafan1_data_frames[i]["Hips"]`。
- 临时为了排查 `FootGroundContactLimit`/`GeomDistanceLimit` 而加入的调试性代码（人为精简 `self.ik_limits`），验证完毕后未及时清理，导致后续几次"正式"训练/转换实际上仍在这个被精简过的、不完整的环境下运行。
- `TerminationsCfg` 里的阈值数值，讨论中已确认改成 0.4，但本地某次留存的文件仍是旧版本的 0.25。
- 新增的诊断函数（`ankle_torque_saturation_diag` 等）随配置文件（`tracking_env_cfg.py`）一起被引用，但对应定义这些函数的源文件（`rewards.py`）未同步更新，导致启动训练时报 `AttributeError`。

这类问题不属于"逻辑 bug"，但实际造成的时间损耗不小（多次需要重新交叉核对哪个文件是"当前生效"的版本）。经验教训是：**涉及多个互相依赖的文件同时修改时，应当以"这次改动涉及的文件是否全部来自同一轮交付"为准绳去部署，任何一个文件单独回退或使用旧版本，都可能导致引用它的其他文件报错或静默地退回到已修复过的旧行为。**

---

## 第四部分：当前状态与后续建议

**已经确认彻底解决、可视为长期基础设施修复的问题**
- `dex_evt2` 错误复用 `dex_evt` IK 配置的问题（1.3）
- 速度限幅数值来源与准确性（1.4）
- 根部重置坐标系问题（1.1）
- 内部状态同步（`configuration.update()`，1.6）
- 六项追踪 reward 与 `torque_smooth` 的 `std` 饱和问题（2.2、2.4、2.5）
- `torque_over_limit` 量级失衡（2.3）
- `_last_torque` 重置（2.1）
- 关节空间速度追踪信号缺失（2.4）

**已确认存在但选择"绕开"而非根治的问题**
- mink 库 `VelocityLimit` 在浮动基座模型上失效（1.5）——已通过事后裁剪机制在数值上兜底，根本原因待向上游反馈。

**仍在验证、尚未有最终定论的问题**
- `entropy_coef` 的最优取值（2.6），目前采用 0.01 与课程学习组合的方案，效果待新一轮训练数据确认。
- 终止阈值课程学习中 `anchor_pos` 部分收窄后的实际效果（2.8）。
- 踝关节物理限位是否是 `ee_body_pos` 问题的部分成因（2.7 中部署的诊断机制，数据待回收）。

**建议的后续动作**
1. 优先在 `aiming1_subject1` 上跑通、验证 `entropy_coef=0.01` + 收窄后课程学习这一组合，确认 `Mean action std` 曲线健康、追踪误差持续下降后，再考虑投入更多时间挑战 `dance1_subject2` 或其他高难度动作。
2. 待时间充裕时，把 mink `VelocityLimit` 的问题整理成正式 issue 提交给上游项目。
3. 若后续需要真正用满 `dex_evt2` 全部 31 个自由度（含头部、手腕后三个关节）做训练，需要在 IsaacLab 侧的机器人配置（`dex_evt.py`）里补全这几个关节的执行器参数（PD 增益等），目前这部分是被注释掉、未启用的状态。
